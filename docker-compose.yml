version: '3.8'

services:
  bot:
    build: .
    container_name: heathen_ledger_bot
    restart: unless-stopped
    volumes:
      - bot_data:/app/data
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/ledger.db

volumes:
  bot_data:
